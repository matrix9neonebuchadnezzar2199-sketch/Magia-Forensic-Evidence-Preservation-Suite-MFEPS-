# --- Optional: GPL-3.0 (CSS decryption, see THIRD_PARTY_LICENSES.md) ---
# pydvdcss>=1.4.0
#
# To enable CSS decryption, uncomment the line above or install manually:
#   pip install pydvdcss>=1.4.0
# When installed, GPL-3.0 terms apply to the pydvdcss component.
# MFEPS functions fully without it (CSS decryption disabled).

# --- Core dependencies (MIT / BSD / Apache compatible) ---
nicegui>=3.8.0
sqlalchemy>=2.0
aiofiles>=23.0
python-dotenv>=1.0
pydantic>=2.0
pydantic-settings>=2.0
psutil>=5.9
reportlab>=4.0
Jinja2>=3.1
rfc3161ng>=2.1.3
wmi>=1.5.1
pywin32>=306
bcrypt>=4.0

# --- Development / testing ---
pytest>=8.0
pytest-cov>=4.0
pytest-asyncio>=0.24
ruff>=0.8.0
pyinstaller>=6.0
httpx>=0.25
playwright>=1.40
pytest-playwright>=0.4
